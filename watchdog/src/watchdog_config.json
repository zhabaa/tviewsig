{
  "telegram_api_id": "${TELEGRAM_API_ID}",
  "telegram_api_hash": "${TELEGRAM_API_HASH}",
  "api_endpoint": "${WATCHDOG_API_ENDPOINT}",
  "api_key": "${API_KEY}",
  "channels": {
    "TOTAL": -1002487581313,
    "BITCOIN": -1002319362918,
    "ETH": -1002396019112
  },
  "parse_rules": {
    "skip_keywords": [
      "pin",
      "закреп",
      "pinned",
      "admin",
      "реклама",
      "#Report",
      "LongReport"
    ],
    "bot_names": {
      "default": "Telegram_Watchdog",
      "channel_mapping": {
        "TOTAL": "TSB TOTAL",
        "BITCOIN": "TSB BTC AI",
        "ETH": "TSB ETH AI"
      }
    }
  },
  "monitoring": {
    "check_interval": "${WATCHDOG_CHECK_INTERVAL}",
    "retry_attempts": "${WATCHDOG_RETRY_ATTEMPTS}",
    "retry_delay": "${WATCHDOG_RETRY_DELAY}"
  }
}
